name: Build Package
on: [push]
jobs:
  Build-Package:
    runs-on: ubuntu-latest
    steps:
      - run: echo "😄 Iniciando build para ${{ github.ref }}."

      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup node
        uses: actions/setup-node@v3

      - name: Install
        run: npm install
        run: echo "🔥 Rodando build."

      - name: Build
        run: npm run build

      - run: ls -la
      
      - name: Pushing lib folder
        uses: actions/checkout@v3
        run: |
          git config --global user.email "thumendess@gmail.com"
          git config --global user.name "Arthur Mendes"
          git add .
          git commit -m "Build para ${{ github.sha }} (auto-generated)"
          git push
